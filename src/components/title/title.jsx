export const Title = (props) => {
    const {title} = props;
    return(
        <>
        
            <div className="font-[600] text-[16px] text-[#27272a]">
                {title}
            </div>
        </>
    )
}